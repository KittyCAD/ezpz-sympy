# ezpz-sympy

This is a collection of residual equations for constraints in [ezpz](https://github.com/KittyCAD/ezpz).

It generates Rust code for residuals and their partial derivatives.

This repo uses `uv`, so please don't use normal `python` or `python3` executables.

## Getting started

Run `uv run main.py`
